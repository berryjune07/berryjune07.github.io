file:

  ```yaml
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  push:
    branches:
name: Deploy Jekyll with GitHub Pages dependencies pre      - main

jobs:
  build:
    runs-on: ubuntu-latestinstalled

on:
  push:
    branches:
      - main



    steps:
      - uses: actions/checkout@v2

      - name: Set up Ruby
        uses: actions/setup-rjobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actionsuby@v1
        with:
          ruby-version: 2.7

      - name: Install dependencies
        run: |
          gem install bundler
          bundle install

      - name:/checkout@v3

      - name: Set up Jekyll
        uses: Build site
        run: bundle exec jekyll build

      - name: Upload artifact
        uses: actions/upload-artifact actions/configure-pages@v3

      - name: Build Jekyll@v2  # Update this version to v2 or the latest supported one site
        uses: actions/jekyll-build-pages@v1


        with:
          name: site
          path: ./_site

      - name: Deploy
        uses: peaceiris/actions    - name: Upload artifact
        uses: actions/upload-art-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dirifact@v4  # Updated from v3 to v4
        with:
  : ./_site